#include<stdio.h> //this is a must important thing to right for running the code 
// this is second step to create a main function in which the code written will be executed
int main(){ 
    //now we take the sample of variable and print it to the screen 
    int a=10;
    int b=20;
    int c= a+b;
    //printf is also the important function to print the value and anything on the screen
    printf("the value of c is %d",c);
    // this is signify the end of the function and also that the functrion will not return any value 
    return 0; 
}`