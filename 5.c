#include<stdio.h>
int main(){
    int a=10;
    int b;
    //assignment operator is used to assign the value to the variable 
    b=a;
    // now the above statement the value of a is assigned to b
    printf("the value of b is %d",b);
    return 0; 
}